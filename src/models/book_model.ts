import { Book } from "./book"

class BookModel {
    public getAll(bibleId: string): Book[] {
        //todo
        return []
    }

    public getById(id: string): Book | null {
        //todo
        return null
    }
}